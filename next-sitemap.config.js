/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://360impex.com', // ← apni real website URL yahan likhein
  generateRobotsTxt: true,
  sitemapSize: 7000,
};

module.exports = config;
